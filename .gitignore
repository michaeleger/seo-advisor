# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# ── Secrets & credentials (NEVER commit) ─────────────────────────────────────
.env
.env.*
!.env.example
client_secret.json
token.json
ads_token.json
credentials.json
*.pem
*.key
.oauth_state
# One-shot OAuth helper artifacts (contain client_id + stale state/PKCE)
ads_oauth_url.txt
open_ads_oauth.html

# Local runtime / generated
seo_state.db
*.db
*.bak
*.log
reports/*.md
reports/*.json
# Keep example HTML out of force-push accidents; reports are regenerable
reports/seo_report_*.html
reports/llm_raw_fail_*.txt
# Per-run page snapshots (copies of live site content)
reports/pages_*/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
